<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'YouTube Link Tracker' %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <%- style %>
</head>
<body class="bg-gray-100 min-h-screen">
    <nav class="bg-white shadow-lg">
        <div class="max-w-6xl mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <a href="/" class="text-2xl font-bold text-blue-600">
                    <i class="fab fa-youtube mr-2"></i>YouTube Link Tracker
                </a>
                <div class="flex space-x-4">
                    <a href="/history" class="text-gray-600 hover:text-blue-600">
                        <i class="fas fa-history mr-2"></i>History
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-4 py-8">
        <%- body %>
    </main>

    <footer class="bg-white shadow-lg mt-8">
        <div class="max-w-6xl mx-auto px-4 py-4 text-center text-gray-600">
            <p>&copy; <%= new Date().getFullYear() %> YouTube Link Tracker. All rights reserved.</p>
        </div>
    </footer>

    <%- script %>
</body>
</html> 